<!--コメントフォーム-->
      <div>
        <%= form_with model: [post, comment], local: false do |f| %>
        <%= f.text_area :comment, rows: '5', placeholder: "コメントをここに", class: 'w-100' %>
        <%= f.submit "送信する", class: "btn btn-sm btn-info rounded-pill" %>
        <% end %>
      </div>